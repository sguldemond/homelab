apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tailscale-ingress
  namespace: argocd
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: argocd-server
      port:
        name: https
  tls:
    - hosts:
        - argocd
